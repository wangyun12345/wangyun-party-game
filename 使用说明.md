# 聚会桌游使用说明

## 访问网址

部署完成后，使用 Vercel 项目页面中显示的 **Production Domain**，格式通常为：

```text
https://<你的项目名>.vercel.app
```

当前代码仓库：[wangyun-party-game](https://github.com/wangyun12345/wangyun-party-game)

如果项目绑定了自定义域名，直接使用自定义域名即可。Vercel 控制台路径：

```text
Project → Deployments → Production → Visit
```

## 首次配置

在 Vercel 项目的 Production 环境中绑定 Upstash Redis，并确认存在以下环境变量：

```text
UPSTASH_REDIS_REST_URL
UPSTASH_REDIS_REST_TOKEN
```

没有 Redis 配置时，页面可能可以打开，但创建房间会提示“房间存储尚未配置”。

## 开始游戏

1. 打开启动网址。
2. 输入昵称，点击“创建房间”。
3. 将页面显示的分享链接或二维码发给朋友。
4. 朋友打开链接后输入昵称加入；也可以手动输入 6 位房间码。
5. 组织者在等待大厅选择“阿瓦隆”或“鹅鸭杀”，确认人数后点击开始。

## 中途刷新或断线

- 同一浏览器会按房间保存匿名席位凭据。
- 刷新房间链接后会自动恢复原席位，不会重复加入。
- 网络短暂中断时，页面会显示“正在重连”，恢复后会重新读取最新房间状态。
- 清除浏览器网站数据后，原席位凭据无法恢复，需要重新加入。

## 离开房间

在等待大厅点击“离开房间”会删除当前席位并使当前凭据失效。游戏开始后不能通过普通离开按钮释放席位。

## 常见问题

### 页面能打开，但无法创建房间

检查 Vercel Production 环境是否绑定 Upstash Redis，并重新部署一次。环境变量修改后，旧部署不会自动使用新配置。

### 页面显示旧内容

在 Vercel 的 Deployments 中确认最新 `dev` 分支提交已完成部署，然后使用无痕窗口或强制刷新。

### 房间链接打不开

确认链接中的房间码为 6 位数字，并检查 Vercel 部署状态是否为 Ready。房间长期无人操作后会自动过期，需要重新创建。

## 开发者本地验证

在项目根目录执行：

```bash
npm install
npm run build
npm run typecheck:server
npm test
npm run lint
```

本地启动 Vercel 开发服务：

```bash
npm run dev
```

本地开发同样需要设置 Upstash Redis 环境变量。
