{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && npm install --prefix party-game",
  "buildCommand": "npm run build",
  "outputDirectory": "party-game/dist",
  "rewrites": [
    { "source": "/room/:code", "destination": "/index.html" }
  ]
}
